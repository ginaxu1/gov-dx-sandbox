# WSO2 Choreo Component Configuration for Policy Decision Point
# +required The configuration file schema version
schemaVersion: 1.2

# +optional Build configuration
build:
  # +required Build type
  type: docker
  # +optional Docker build context (relative to repository root)
  context: exchange/policy-decision-point
  # +optional Dockerfile path (relative to build context)
  dockerfile: Dockerfile
  # +required Build arguments
  args:
    - name: SERVICE_PATH
      value: policy-decision-point
    - name: BUILD_VERSION
      value: latest
    - name: BUILD_TIME
      value: ""
    - name: GIT_COMMIT
      value: ""

# +optional Incoming connection details for the component
endpoints:
  # +required Unique name for the endpoint.
  - name: policy-decision-point-api
    # +optional Display name for the endpoint.
    displayName: Policy Decision Point API
    # +required Service section has the user service endpoint details
    service:
      # +optional Context (base path) of the API that gets exposed via the endpoint.
      basePath: /
      # +required Numeric port value that gets exposed via the endpoint
      port: 8082
    # +required Type of traffic that the endpoint is accepting.
    type: REST
    # +optional Network level visibilities of the endpoint.
    networkVisibilities: 
      - Public
      - Organization

# +optional Outgoing connection details for the component.
dependencies:
  # +optional Defines the connection references from the Internal Marketplace.
  connectionReferences:
    # +required Name of the connection.
    - name: database-connection
      # +required service identifer of the dependent component.
      resourceRef: service:/DatabaseProject/PostgreSQLComponent/v1/ad088/PUBLIC

# +optional Defines runtime configurations
configurations:
  # +optional List of environment variables to be injected into the component.
  env:
    # +required Name of the environment variable
    - name: ENVIRONMENT
      # +required value source
      valueFrom:
        # +required config form value source
        configForm:
          # +optional display name inside the config form
          displayName: Environment
          # +optional default value is true if not specified
          required: true
          # +optional default value is string if not specified
          type: string
    - name: PORT
      # +required value source
      valueFrom:
        # +required config form value source
        configForm:
          # +optional display name inside the config form
          displayName: Port
          # +optional default value is true if not specified
          required: false
          # +optional default value is string if not specified
          type: number
    - name: LOG_LEVEL
      # +required value source
      valueFrom:
        # +required config form value source
        configForm:
          # +optional display name inside the config form
          displayName: Log Level
          # +optional default value is true if not specified
          required: false
          # +optional default value is string if not specified
          type: string
    - name: LOG_FORMAT
      # +required value source
      valueFrom:
        # +required config form value source
        configForm:
          # +optional display name inside the config form
          displayName: Log Format
          # +optional default value is true if not specified
          required: false
          # +optional default value is string if not specified
          type: string
    - name: OPA_POLICIES_PATH
      # +required value source
      valueFrom:
        # +required config form value source
        configForm:
          # +optional display name inside the config form
          displayName: OPA Policies Path
          # +optional default value is true if not specified
          required: false
          # +optional default value is string if not specified
          type: string
    - name: OPA_DATA_PATH
      # +required value source
      valueFrom:
        # +required config form value source
        configForm:
          # +optional display name inside the config form
          displayName: OPA Data Path
          # +optional default value is true if not specified
          required: false
          # +optional default value is string if not specified
          type: string
    - name: ENABLE_DEBUG
      # +required value source
      valueFrom:
        # +required config form value source
        configForm:
          # +optional display name inside the config form
          displayName: Enable Debug Mode
          # +optional default value is true if not specified
          required: false
          # +optional default value is string if not specified
          type: boolean